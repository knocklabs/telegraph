import { Meta } from "@storybook/addon-docs/blocks";
import { ColorsGrid } from "../components/ColorsGrid";

<Meta title="Tokens/Colors" />

# Colors

Telegraph uses a semantic color system built on perceptual 12-step scales, with automatic light and dark theme support.

<ColorsGrid />
