import { Meta } from "@storybook/addon-docs/blocks";
import { RadiusGrid } from "../components/RadiusGrid";

<Meta title="Foundations/Radius" />

# Radius

Telegraph provides 8 border radius tokens — from sharp edges to fully circular — available as `--tgph-rounded-*` CSS variables.

<RadiusGrid />
