import { Meta } from "@storybook/addon-docs/blocks";
import { SpacingTable } from "../components/SpacingTable";

<Meta title="Tokens/Spacing" />

# Spacing

Telegraph uses a spacing scale based on a 4px grid. Spacing tokens are available as CSS variables prefixed with `--tgph-spacing-`.

<SpacingTable />
